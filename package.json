{
  "name": "mri-scan-viewer",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "encrypt-scans": "node scripts/encrypt-scans.js",
    "generate-index": "node scripts/generate-scan-index.js",
    "predev": "npm run encrypt-scans && npm run generate-index",
    "prebuild": "npm run encrypt-scans && npm run generate-index",
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^8.1.4"
  },
  "dependencies": {
    "cornerstone-core": "^2.3.0",
    "cornerstone-wado-image-loader": "^4.1.3",
    "dicom-parser": "^1.8.21"
  }
}
